# AGENTS.md

## 项目背景

- 本项目是一个基于 Next.js 与 Supabase 构建的 Web 应用。
- 后续开发应优先沿用项目当前已有的技术栈、目录结构、依赖选择和代码风格。
- 修改代码前先阅读相关实现，避免无必要地引入新的框架、重复依赖或与现有结构冲突的抽象。

## 项目文档目录约定

项目相关文档统一放在 `/docs/` 目录下：

- `/docs/prd.md`：存放项目需求文档、功能规格、已确认的产品决策，以及尚待确认的问题。
- `/docs/images/`：存放截图、界面参考图、流程图等图片资料。
- `/docs/API/`：存放 API 文档、接口约定和第三方服务对接说明。

新增文档时，应放入与内容相符的上述目录，不要将项目文档散落在源码目录中。

## 生图模型 API 文档索引

实现或修改生图模型接入前，必须先阅读对应的本地 API 文档，并优先以文档中定义的接口、参数、返回结构和限制为准：

- GPT Image2 文生图：`/docs/API/gpt-image2-text2img.md`
- GPT Image2 图生图：`/docs/API/gpt-image2-img2img.md`
- Nano Banana2 生图：`/docs/API/nano-banana.md`

如果代码实现与文档不一致，或文档缺少完成任务所需的信息，应先指出差异或缺口，不得凭猜测补造接口字段、模型参数或返回格式。

## 需求沉淀规则

- 当与用户讨论功能实现、产品需求、业务规则或交互细节时，应及时识别已经明确并确认的需求。
- 已确认的需求应整理后写入 `/docs/prd.md`，保持文档与当前产品决策同步。
- 写入 PRD 的内容必须以用户已经确认的信息为依据，不得把未经确认的推测、默认假设或实现偏好写成最终需求。
- 对尚不完整、存在歧义或需要用户决策的事项，可以记录在“待确认问题”或“开放问题”章节中，并清楚标注其状态。
- 更新 PRD 时应合并到合适的现有章节，保留仍然有效的内容，避免重复记录或无意义覆盖。

## Supabase 操作规则

- 执行 Supabase 相关任务时，应尽可能优先使用 Supabase MCP 查看或操作当前项目。
- 适合优先通过 Supabase MCP 完成的任务包括但不限于：
  - 查看数据库表结构、字段、索引和迁移记录。
  - 执行 SQL 查询。
  - 检查、设计和创建 RLS 策略。
  - 查看和创建 Storage Bucket。
- 执行写入、迁移、策略变更或资源创建前，应核对目标项目和操作范围；高影响或不可逆操作需要先获得用户明确确认。
- 如果 Supabase MCP 不可用或权限不足，应说明具体限制，再选择安全且范围明确的替代方式。

## 安全和配置约定

- 禁止将 Supabase service role key、数据库密码、个人访问令牌及其他敏感信息写入前端代码、日志、文档或提交到仓库。
- 前端代码只能使用以 `NEXT_PUBLIC_` 开头、明确允许公开的环境变量。
- 服务端密钥只能保存在受保护的服务端环境变量中，不得通过客户端 bundle、公开接口或错误信息泄露。
- 涉及用户数据访问时，应优先设计和检查 RLS 策略，并遵循最小权限原则。
- 不得因为前端隐藏了入口就假定数据安全；访问控制必须在数据库或可信服务端边界执行。
- 修改环境变量约定时，应同步维护 `.env.example`，其中只能使用占位值，不得写入真实凭据。

## 编辑原则

- 修改已有文件时，保留仍然有效的原有内容，并以最小必要改动完成任务。
- 不要无意义覆盖 `AGENTS.md`、PRD 或其他项目文档。
- 新增或调整目录、约定和重要实现决策时，应同步更新相关文档。
